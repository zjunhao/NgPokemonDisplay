import { UpperFirstCharacterPipe } from './upper-first-character.pipe';

describe('UpperFirstCharacterPipe', () => {
  it('create an instance', () => {
    const pipe = new UpperFirstCharacterPipe();
    expect(pipe).toBeTruthy();
  });
});
